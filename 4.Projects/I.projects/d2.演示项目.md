---
project: d2.演示项目
type: main-project
priority: high
status:
  - 进行中
tags:
  - project
description: 简介描述
start-date: 2026-05-14
end-date:
---

# d2.演示项目

> [!note] 这是一个项目示例
> 一级项目 + 下面的子任务，会被 Dataview 自动汇总到这里和总览页。

## task执行表
```dataview
TABLE
  completion as "完成度",
  mood as "心情",
  date as "日期",
  详情 as "详情"
FROM "4.Projects/II.tasks"
WHERE project = "d2.演示项目"
SORT seq DESC
```

## 任务

### 未完成任务
```dataview
TASK
FROM "4.Projects/I.projects/d2.演示项目"
WHERE !completed
```
### 主要任务
- [ ] 示例1
- [x] 示例2  [completion:: 2026-05-15]
### 暂缓任务
- [ ] 示例3

## 统计

### 进度统计
```dataviewjs
const tasks = dv.pages('"4.Projects/II.tasks"')
  .where(p => p.project === "d2.演示项目");

const total = tasks.length;
const avgCompletion = tasks.array()
  .reduce((sum, t) => sum + (t.completion || 0), 0) / total;

dv.paragraph(`
**总任务数**: ${total}  
**平均完成度**: ${avgCompletion.toFixed(1)}/10  
**本周任务**: ${tasks.where(t => {
  const weekAgo = dv.date('now').minus({days: 7});
  return t.date >= weekAgo;
}).length}
`);
```

### 日历图
```dataview
CALENDAR date
FROM "4.Projects/II.tasks"
WHERE project = "d2.演示项目"
```

> [!tip] 👉 轮到你
> 用 `Cmd+'`（或项目模板）建一个你自己的真实项目；上手之后把这个示例项目删掉就行。
